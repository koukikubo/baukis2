<% @title = "職員の新規登録" %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_with model: @staff_member, url: [ :admin, @staff_member ] do |f| %>
    <%= render "form", f: f %>  <%#  f: f  左のfは_form部分テンプレートで参照するfであり、右のfはブロック変数のf %>
    <div class="buttons">
      <%= f.submit "登録"%>
      <%= link_to "キャンセル", :admin_staff_members %>
    </div>
  <% end %>
</div>
